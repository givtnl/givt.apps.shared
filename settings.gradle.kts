include(":GivtCodeShare")
pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "givtappsshared"
include(":androidApp")
include(":GivtCodeShare")